<% if can? :read, :menu %>
  <%= link_to "#{t('list')} #{t('activerecord.models.product').pluralize}", admin_products_path %> |
  <%= link_to "#{t('list')} #{t('activerecord.models.auction').pluralize}", admin_auctions_path %> |
  <%= link_to "#{t('list')} #{t('activerecord.models.bid').pluralize}", admin_bids_path %>
  <br />
<% end %>
