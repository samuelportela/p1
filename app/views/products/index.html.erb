<h1><%= t('activerecord.models.product').pluralize %></h1>

<%= link_to t(:new), new_product_path %>

<ul>
  <% for product in @products %>
  <li>
    <%= product.name %>
    | <%= link_to t(:edit), edit_product_path(product) %>
  </li>
  <% end %>
</ul>
